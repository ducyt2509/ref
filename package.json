{
  "name": "referral-reward-calculator",
  "version": "1.0.0",
  "description": "A TypeScript application to calculate referral rewards based on levels",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "bignumber.js": "^9.2.1"
  }
}
